{
  "name": "@veecode/spacelift-io-backstage-plugins",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "yarn workspace @veecode/spacelift-io-shared run build && yarn workspaces foreach -A --exclude @veecode/spacelift-io-shared -p run build",
    "start": "yarn workspaces foreach -A -p run start",
    "clean": "yarn workspaces foreach -A -p run clean",
    "yalc:publish:all": "yarn workspaces foreach -A -p run yalc:publish",
    "yalc:watch:all": "yarn workspaces foreach -A -p run yalc:watch",
    "test": "yarn workspaces foreach -A -p run test --all --watchAll=false",
    "test:ci": "yarn workspaces foreach -A -p run test --all --watchAll=false --ci --coverage"
  },
  "devDependencies": {
    "eslint": "^8.0.0",
    "typescript": "~5.4.5"
  },
  "packageManager": "yarn@4.9.1"
}
